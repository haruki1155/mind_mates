class AssessmentSafetyConfig {
  const AssessmentSafetyConfig._();

  /// Must remain false until PACC and the research team approve the complete
  /// protocol and every production contact has been independently verified.
  static const bool safetyScreeningEnabled = false;
}
